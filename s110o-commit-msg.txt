Sprint 11.0O - REMOVE Dart-side fake UI animation (S86 audit, S28 inverted)

Owner 13:20 CRITICAL FINDING: the active pool screen shows
animated packet + volunteer counts even when the VPN is NOT
started. The Kotlin side was correct (Sprint 11.0M audit
dump proved packetsObserved.incrementAndGet has exactly one
site in startReaderThread, in the real read branch). The
Dart side still had Sprint 10.1A mock ticker leftover code
that was never cleaned up. 11.0A-C said "retired" but
the timers, mock initial values, and fake callback stayed.

11.0O removes three sources of fake animation:

  1. pool_provider.dart:
     - REMOVED Timer.periodic _mockTimer (3-second mock
       ticker that bumped paketSayisi + gonulluSayisi).
     - REMOVED _mockTick() function body.
     - REMOVED _mockTickPeriod constant.
     - REMOVED the "dispose" cancel of _mockTimer.
     - REPLACED with real data flow: paketSayisi accumulates
       from _vpn.getSampledPackets().length (cumulative
       count of TUN-reader SampledPackets); gonulluSayisi is
       peers.length from _matcher.findActiveReceivers(...)
       (the canonical P2P matcher endpoint at
       GET /api/v1/sessions, no mock 2-5 fallback).
     - PoolState.initial() now returns 0/empty (was
       247/3/{rcs,whatsapp}/[1,2,1,...]).

  2. active_pool_screen.dart:
     - REMOVED Future.delayed(5s) "Eşleşme bulundu!" fake
       snackbar (it fired regardless of any backend
       response).
     - REMOVED the _eslesmeZamanlayiciAktif field
       (no longer needed).
     - REMOVED unused flutter/foundation.dart and
       flutter/services.dart imports (HapticFeedback
       was only used in the removed fake snackbar).
     - The match-found snackbar is now driven by
       ref.listen(lastSuccess) in build() — the real
       backend response path.

  3. S28 audit INVERTED:
     - Pre-11.0O: S28 enforced Timer.periodic MUST be
       present in pool_provider.dart (the mock ticker
       literal).
     - 11.0O: S28 enforces Timer.periodic(... => _mockTick)
       MUST NOT be present (the mock callback). The
       legitimate 5s _apiTick poll Timer.periodic IS
       allowed.
     - The 2 S28 selftest cases were re-flipped to match
       the new direction (PASS = no mock ticker, FAIL =
       mock ticker present).

  4. S86 audit (new):
     - Verifies active_pool_screen.dart + pool_provider
       .dart + state/*.dart have NO mock Timer.periodic
       callback, NO setInterval, NO Future.delayed.
     - Verifies the screen DOES subscribe to
       _vpn.packetStream.listen and _vpn.stateStream
       .listen (the real VPN streams).
     - The legitimate 5s _apiTick poll Timer.periodic IS
       allowed (S86 distinguishes real API callbacks
       from mock callbacks by name).

Verification:
  - 136/136 audit-self-test (135 + 1 S86 added; the
    previous 11.0N S85 was cancelled).
  - 37/37 flutter test PASS.
  - S28 (inverted) + S86 production audits PASS, 0
    new findings.
  - flutter analyze: No issues found.
  - Fresh APK SHA captured in s110o-final-report.md.

Owner test: open the active pool screen WITHOUT starting
the VPN — the paketSayisi and gonulluSayisi counters
should now show 0 0 and stay at 0 0 until the first
real API poll returns (5s) or the first real VPN packet
arrives (after tapping "Şifreleme Doğrulamayı Başlat").
